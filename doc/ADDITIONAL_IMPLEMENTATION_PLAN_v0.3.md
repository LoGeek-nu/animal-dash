# ANIMAL DASH! 追加修正用実装計画 v0.3

- 文書バージョン: 0.3
- 作成日: 2026-08-19
- 対象: 学園祭・展示イベント向け 2Dレースゲームモック
- 前提バージョン: v0.2 実装済み
- 対象画面: `/game`、`/admin`、API、Worker、ビルド・公開設定

---

## 1. 今回の目的

v0.3では、現在の見た目とレース機能を維持しながら、プロジェクトの実装言語をJavaScriptへ統一し、認証を完全に取り除く。同時に、展示当日のスタッフ操作とゲーム画面の見やすさを改善する。

今回の主要変更は次の5項目とする。

1. TypeScript / TSXを廃止し、すべてJavaScript / JSXへ移行する
2. ChatGPT認証コードとホスティング側のサインイン制限を撤去する
3. 管理画面にキャラクターのドラッグ&ドロップ操作を追加する
4. レースコースを、奥行きと区間変化のある学園祭コースへ再設計する
5. 参加待機カードを縦長にし、イラスト・名前・能力バーが重ならない構造へ変更する

本書で参照する添付画像は、現在の待機カードで発生している「イラストと能力表示の重なり」を確認するための視覚資料として扱う。

---

## 2. 実装方針

### 2.1 維持するもの

- React 19、Vinext、Vite、Cloudflare Worker互換の構成
- `/game`、`/admin`、`/api/characters`、`/api/rankings`、`/health`
- アトラクト画面、参加待機、カウントダウン、レース、結果のフェーズ構成
- 10体の手描きキャラクター素材
- `BroadcastChannel` と `localStorage` による同一端末内同期
- キーボード、Gamepad API、BOT操作
- 現在のクレヨン・ガッシュ調のアートディレクション

### 2.2 変更しないもの

- この段階ではサーバーDB、ユーザーアカウント、オンライン対戦を追加しない
- 認証を別方式へ置き換えない
- レースの基本ルール、4レーン、ジャンプ、ブーストは変更しない
- キャラクター画像の再生成は行わず、既存素材を再利用する

### 2.3 v0.3完了時の構成

- アプリケーションソース: `.js` / `.jsx`
- 設定ファイル: `.js` / `.mjs` / `.json`
- TypeScriptコンパイラと型定義用ツール: 不使用
- アプリ認証: なし
- 公開サイトのアクセス制限: なし
- 管理画面操作: ドラッグ&ドロップを主操作、クリックを代替操作として維持

---

## 3. 現状調査と問題点

### 3.1 JavaScript化の対象

現状、主要機能はTypeScript / TSXで実装されている。

| 現在 | v0.3 | 対応 |
|---|---|---|
| `app/AnimalDashApp.tsx` | `app/AnimalDashApp.jsx` | 型注釈と型キャストを除去 |
| `app/race-data.ts` | `app/race-data.js` | 型定義をJSDocへ移行 |
| `app/use-race-session.ts` | `app/use-race-session.js` | 型ガードを通常の検証関数へ変更 |
| `app/layout.tsx` | `app/layout.jsx` | `Metadata`型、props型を除去 |
| `app/page.tsx` | `app/page.jsx` | JSXとして移行 |
| `app/game/page.tsx` | `app/game/page.jsx` | JSXとして移行 |
| `app/admin/page.tsx` | `app/admin/page.jsx` | JSXとして移行 |
| `app/**/route.ts` | `app/**/route.js` | APIをJavaScript化 |
| `worker/index.ts` | `worker/index.js` | Worker型を実行時チェック/JSDocへ移行 |
| `vite.config.ts` | `vite.config.js` | Workerエントリも`.js`へ変更 |
| `next.config.ts` | `next.config.js` | `NextConfig`型を除去 |
| `drizzle.config.ts` | 削除 | 現在D1未使用のため不要 |
| `db/**/*.ts` | 削除 | 未使用のスターターDBコードを撤去 |
| `examples/d1/**/*.ts` | 削除 | 製品コードではないサンプルを撤去 |
| `app/chatgpt-auth.ts` | 削除 | 認証撤去 |
| `next-env.d.ts` | 削除 | TypeScript廃止 |
| `tsconfig.json` | 削除 | TypeScript廃止 |

`public`配下のPNG、MP4、SVGと、CSS、Markdown、JSONはそのまま維持する。

### 3.2 認証の現状

認証は2層に分かれている。

1. アプリコード
   - `app/chatgpt-auth.ts` にChatGPT認証ヘッダー、サインイン、サインアウト処理が存在する
   - 現在の画面からは直接呼ばれていないため、削除によるゲーム機能への影響はない
2. 公開設定
   - 現在の公開サイトは所有者限定のアクセス設定になっている
   - コードだけ削除しても、公開URLではChatGPTサインインが残る

したがって、完全撤去にはコード削除と公開設定変更の両方を行う。

### 3.3 管理画面の現状

- 左側のキャラクターをクリックする
- 右側のレーンで「このレーンにセット」を押す
- 選択状態が左右に分かれ、操作回数が多い
- ドラッグ中の移動先が視覚的に示されない
- キャラクター一覧とレーンの関係が直感的ではない

### 3.4 レースコースの現状

- 4レーンが同じ単純な背景の繰り返しになっている
- 障害物は岩、丸太、泥の固定4点のみ
- コースの始点・中盤・終盤の変化が弱い
- 学園祭らしいランドマークや観客、旗、ゴールへの期待感が不足している
- 背景スクロールが1層で、奥行きが感じにくい

### 3.5 待機カードの現状

添付画像では、キャラクターの足元とSPD / ACC / JMP / STMバーが重なっている。

原因は次の通り。

- カードの高さに対してキャラクター画像の占有率が大きい
- イラスト領域と能力表示領域が別レイアウトになっていない
- 画面幅によって画像が能力表示側へ入り込む
- カード内の余白が固定値中心で、縦横比の変化に弱い

---

## 4. JavaScript / JSX全面移行

### 4.1 移行原則

- 画面デザインの変更前に、同じ表示・同じ動作のJS版を成立させる
- ファイル名変更と機能変更を同じ差分で大量に混在させない
- TypeScript型は、必要な箇所のみJSDoc `@typedef`、`@param`、`@returns`へ移す
- ブラウザから受け取る値は、型の代わりに実行時検証を行う
- `as React.CSSProperties`などの型キャストはすべて除去する

### 4.2 データモデル

`race-data.js` に次のJSDoc定義を置く。

- `RacePhase`
- `CharacterStats`
- `RaceCharacter`
- `LaneAssignment`
- `RaceResult`
- `RaceSession`

JSDocはエディタ補完のために使用し、TypeScriptコンパイルは行わない。

### 4.3 セッション検証

`validSession()` は次を実行時に確認する。

- オブジェクトであること
- `version` が現在値と一致すること
- `phase` が許可された文字列であること
- `lanes` が4要素であること
- 各レーンの `characterId` が登録済みIDであること
- `sequence` と時刻フィールドが妥当な数値であること

壊れた保存データは初期状態へ戻し、画面を停止させない。

### 4.4 パッケージ整理

削除対象:

- `typescript`
- `typescript-eslint`
- `@types/node`
- `@types/react`
- `@types/react-dom`
- `drizzle-kit`
- `drizzle-orm`（v0.3でDBコードを削除するため）
- `db:generate` スクリプト

ESLintはJavaScript / JSX向け設定だけを残し、React Hooks、アクセシビリティ、Next互換ルールは維持する。

### 4.5 JS移行の完了条件

- 製品リポジトリ内に `.ts`、`.tsx`、`.d.ts` が残っていない
- `tsconfig.json` が存在しない
- `package.json` にTypeScript関連依存がない
- `/game`、`/admin`、全APIが移行前と同じURLで動作する
- `npm run build` と `npm test` が成功する

---

## 5. 認証の完全撤去

### 5.1 コードから削除するもの

- `app/chatgpt-auth.ts`
- `signin-with-chatgpt`、`signout-with-chatgpt`、`callback`への参照
- `oai-authenticated-user-*` ヘッダーの参照
- ユーザー情報を前提にした型、関数、文言
- 認証用の将来コードや未使用import

### 5.2 公開設定

公開時にサイトのアクセスモードを `public` へ変更する。

- `/game` を匿名ブラウザで直接開ける
- `/admin` も認証なしで直接開ける
- ChatGPTサインイン画面へ転送されない
- APIとヘルスチェックも認証なしで応答する

### 5.3 認証なし運用の注意点

管理画面も公開になる。ただしv0.3の状態共有は同一ブラウザ内の `localStorage` / `BroadcastChannel` だけなので、別端末の訪問者がイベント端末のレースを直接操作することはできない。

将来認証を戻す際にUIを作り直さなくて済むよう、管理画面の操作ロジックとアクセス制御は分離する。将来は `/admin` の入口だけを保護できる構造とする。

### 5.4 認証撤去の完了条件

- コード検索でChatGPT認証文字列が0件
- シークレットや認証環境変数を要求しない
- 未ログイン状態で全ルートへアクセスできる
- サインイン・サインアウトリンクが表示されない

---

## 6. 管理画面のドラッグ&ドロップUX

### 6.1 操作方式

`@dnd-kit/core` を使用し、マウス、タッチ、キーボードに対応する。

追加する構成:

- `DndContext`
- `PointerSensor`
- `KeyboardSensor`
- `DragOverlay`
- キャラクター用 `useDraggable`
- 4レーン用 `useDroppable`

### 6.2 基本操作

1. 左側のキャラクターカードを押してドラッグ開始
2. 左側パネルと操作バーを暗くする
3. 右側のレーン管理パネルだけを明るく保つ
4. ドロップ可能なレーンを発光させる
5. カーソル下のレーンを拡大し、「ここにセット」を表示する
6. ドロップ時に一度だけセッションを更新する
7. 配置後、短い着地アニメーションと同期表示を出す

ドラッグ中の見た目は次の3段階とする。

| 状態 | 左パネル | 右パネル | 対象レーン |
|---|---|---|---|
| 通常 | 100% | 100% | 通常 |
| ドラッグ中 | 35%・彩度低下 | 100% | 空きレーンが発光 |
| レーン上 | 25% | 100% | 対象だけ黄色い太枠・1.02倍 |

### 6.3 ドロップ規則

- 空きレーンへのドロップ: そのまま配置
- 使用中レーンへのドロップ: 既存キャラクターと置換し、元キャラクターを一覧へ戻す
- 使用済みキャラクター: 一覧では無効状態にする
- レーン外へのドロップ: 変更せず元の位置へ戻す
- `WAITING` / `ATTRACT` 以外: ドラッグを無効化する
- ドラッグ中は状態を保存せず、`onDragEnd` の成功時だけ保存する

### 6.4 ドラッグ表示

`DragOverlay` には次を表示する。

- キャラクター全身画像
- 名前
- タイプ
- 半透明の白いステッカー背景
- 持ち上がった影と5度程度の傾き

元カードには「移動中」の空きシルエットを残し、どのキャラクターを運んでいるか見失わないようにする。

### 6.5 代替操作とアクセシビリティ

- 現在のクリック選択 → レーンボタン操作を残す
- `Space` で持ち上げ、矢印でレーン移動、`Enter` で配置、`Esc` で中止
- ドロップ結果を `aria-live` で通知する
- 8px程度動かすまでドラッグを開始せず、通常クリックの誤作動を防ぐ
- タッチ操作では縦スクロールと競合しないよう、長押しまたは移動距離を設定する
- `prefers-reduced-motion` では拡大・回転アニメーションを抑える

### 6.6 DnDの完了条件

- 10体すべてを任意の4レーンへドラッグできる
- ドラッグ中、右側ターゲット以外が視覚的に暗くなる
- レーン外ドロップでデータが変化しない
- 置換、解除、BOT補充後も重複キャラクターが発生しない
- マウス、タッチ、キーボードの各操作が成立する
- ゲーム画面へ配置結果が即時同期される

---

## 7. レースマップ再設計

### 7.1 コンセプト

コース名を「桜麗祭フェスティバル・パーク」とし、学校のグラウンドを動物たちが駆け抜ける一続きのコースとして見せる。

単純な横縞ではなく、進行率に応じて景色と障害物が変わる5区間を作る。

| 進行率 | 区間 | 主な景色 | 障害物 |
|---|---|---|---|
| 0–18% | スタートヤード | スタートゲート、応援旗、校舎 | なし・操作確認 |
| 18–40% | フラワーガーデン | 花壇、風車、蝶、看板 | 小岩 |
| 40–62% | ログブリッジ | 木立、丸太橋、小川 | 丸太 |
| 62–82% | マッドフェス | テント、屋台、泥はね | 泥、水たまり |
| 82–100% | ファイナルストレート | 観客、紙吹雪、ゴールアーチ | 最後の低い障害物 |

### 7.2 データ構造

固定数値配列 `obstacles` をオブジェクト配列へ変更する。

各障害物は次を持つ。

- `id`
- `type`: `rock` / `log` / `mud` / `puddle` / `hay`
- `position`
- `hitHeight`
- `width`
- `penaltyMs`
- `visualVariant`
- `warningDistance`

これにより見た目、当たり判定、減速量を同じデータから生成する。

### 7.3 背景レイヤー

各レーン内に次のレイヤーを設ける。

1. 遠景: 空、雲、校舎、山
2. 中景: 木、テント、旗、観客
3. コース: 芝、土、白線、区間模様
4. 前景: 花、草、紙吹雪、泥はね
5. ゲーム要素: キャラクター、障害物、ゴール

レイヤーごとに進行率への移動倍率を変え、遠景は遅く、前景は速く動かす。CSSカスタムプロパティ `--course-progress` と `--parallax-*` を使い、Reactの再レンダーを増やさない。

### 7.4 コース上の演出

- 区間開始時に手描き看板を画面右から入れる
- 障害物の手前に点滅しすぎない注意マークを出す
- ジャンプ時に土煙と速度線を表示する
- 衝突時に星と揺れを表示する
- 泥エリアではキャラクター足元へ泥はねを付ける
- 80%以降は観客、旗、紙吹雪を増やす
- ゴール直前にアーチとチェッカーフラッグを大きく見せる

### 7.5 可読性

- 背景はキャラクターより彩度とコントラストを一段下げる
- レーン色は情報パネル、進行バー、足元ラインに限定する
- 障害物は背景と混ざらない濃い輪郭を付ける
- 4レーンのゲーム情報は常に固定位置へ置く
- 動画・背景演出は `prefers-reduced-motion` に対応する

### 7.6 マップの完了条件

- スタートからゴールまで少なくとも5種類の景観変化がある
- 背景に3段階以上の視差が感じられる
- 障害物の種類と当たり判定がデータで一元管理される
- 全障害物がジャンプ前に視認できる
- キャラクター、順位、ブーストゲージの視認性を損なわない
- 4レーンで同一コース条件が再現される

---

## 8. 参加待機カードの再設計

### 8.1 新しいカード構造

カードを次の3領域へ明確に分割する。

```text
┌─────────────────┐
│ LANE 1      BOT │  ヘッダー
├─────────────────┤
│                 │
│  キャラクター   │  イラスト専用領域
│                 │
├─────────────────┤
│ タイプ          │
│ キャラクター名  │  情報専用領域
│ SPD ━━━━━━━     │
│ ACC ━━━━━       │
│ JMP ━━━━━━      │
│ STM ━━━━━━━     │
└─────────────────┘
```

`WaitingScreen` のマークアップを次へ分割する。

- `.waiting-card-header`
- `.waiting-card-art`
- `.waiting-card-meta`
- `.waiting-card-stats`

### 8.2 サイズ

16:9展示画面では次を基準とする。

- 4列表示
- カードの幅: 利用可能幅の約22–23%
- カードの縦横比: `width / height = 0.68–0.74`
- 最小高さ: 430px
- イラスト領域: カード高の55–60%
- 情報領域: カード高の32–36%
- ヘッダー: 残り8–10%

`object-fit: contain` を使用し、キャラクター画像の下端と能力バーの間に最低16pxの余白を確保する。

### 8.3 能力表示

- 能力バーの背景を半透明ではなく明るい紙色にする
- ラベル幅を固定し、4本の開始位置を揃える
- バー高さを最低7pxにする
- 名前とバーをキャラクター画像より前面へ重ねない
- タイプ色とレーン色を分け、能力バーはキャラクター色を使う
- 数値は必要に応じてバー右端へ `9/10` の形で表示する

### 8.4 レスポンシブ

- 1180px以上: 4列、画面内完結
- 768–1179px: 2列、縦スクロール
- 767px以下: 1列または2列、情報領域を省略しない
- 低い画面ではイラストを縮小し、能力表示を先に守る

### 8.5 待機カードの完了条件

- 1920×1080、1366×768、1024×768で画像と能力バーが重ならない
- 全10キャラクターで耳、尻尾、足がカード外へ不自然に切れない
- 名前、タイプ、4本の能力バーが常に読める
- 空きレーンも同じ縦長サイズを保ち、配置後にレイアウトが跳ねない
- 4人登録時もゲームヘッダーとフッターが見える

---

## 9. コンポーネント分割

現在の `AnimalDashApp.tsx` は複数画面を1ファイルに持つため、JS移行時に次へ分割する。

```text
app/
├─ AnimalDashApp.jsx
├─ components/
│  ├─ character/
│  │  ├─ CharacterAvatar.jsx
│  │  └─ StatBars.jsx
│  ├─ game/
│  │  ├─ AttractScreen.jsx
│  │  ├─ WaitingScreen.jsx
│  │  ├─ CountdownScreen.jsx
│  │  ├─ RaceArena.jsx
│  │  ├─ CourseScenery.jsx
│  │  └─ ResultsScreen.jsx
│  └─ admin/
│     ├─ AdminExperience.jsx
│     ├─ CharacterLibrary.jsx
│     ├─ DraggableCharacter.jsx
│     ├─ LaneDropTarget.jsx
│     └─ CharacterDragOverlay.jsx
├─ race-data.js
├─ course-data.js
└─ use-race-session.js
```

`AnimalDashApp.jsx` はフェーズ切り替えだけを担当し、レース計算、コース表示、管理画面DnDを分離する。

---

## 10. 実装順序

### Phase 0: ベースライン固定

- 現在のJS移行前バージョンでビルドとテストを実行
- `/game` と `/admin` の主要状態を確認
- 現行の公開URLとローカルURLを記録

### Phase 1: 最小JS / JSX化

- `race-data`、セッションフック、ルート、レイアウトをJSへ移行
- `AnimalDashApp` をJSX化
- 見た目と動作を変更せずビルドを通す
- この時点で最初の動作プレビューを確認する

### Phase 2: TypeScript・未使用スターター撤去

- Worker、Vite、Next設定をJS化
- 未使用DB、D1サンプル、Drizzle設定を削除
- TypeScript依存と設定ファイルを削除
- ESLintをJS / JSX専用へ変更

### Phase 3: 認証撤去

- ChatGPT認証ヘルパーを削除
- 認証文字列の残存を検索
- アプリを認証なしでビルド
- 公開設定を `public` へ変更する準備を行う

### Phase 4: 管理画面DnD

- DnDコンテキストとセンサーを追加
- キャラクターカードをドラッグ可能にする
- レーンをドロップターゲット化
- 暗転、発光、オーバーレイ、キャンセル演出を追加
- クリックとキーボードの代替操作を確認

### Phase 5: 待機カード再設計

- カードをヘッダー、イラスト、情報へ分割
- 4列時の縦長比率を確定
- 全キャラクターと代表画面サイズで重なりを確認

### Phase 6: レースマップ再設計

- `course-data.js` を追加
- コース区間、障害物、背景レイヤーを実装
- 当たり判定を新データへ接続
- 視差、警告、泥、ゴール演出を追加

### Phase 7: 統合テストと公開

- lint、build、自動テスト
- ゲーム全フェーズの起動テスト
- DnDのマウス、タッチ、キーボードテスト
- 匿名アクセス確認
- 公開サイトをアクセス制限なしで更新

---

## 11. テスト計画

### 11.1 自動テスト

- `/game` がアトラクト画面をサーバーレンダリングする
- `/admin` がキャラクター一覧と4レーンを表示する
- API 3系統が認証なしで応答する
- `validSession()` が破損データを拒否する
- キャラクター重複配置を防ぐ
- ドロップ成功、レーン外キャンセル、置換が正しく動作する
- コースデータの位置が0–100%内に収まる
- 全障害物に一意なIDがある

### 11.2 起動・操作テスト

1. アトラクト画面を開く
2. 管理画面から待機画面へ切り替える
3. キャラクターを4レーンへドラッグする
4. BOT補充と置換を行う
5. レースを開始する
6. ジャンプ、ブースト、衝突を確認する
7. 5区間を通過してゴールする
8. リザルトからアトラクトへ戻る

### 11.3 認証テスト

- シークレットウィンドウで `/game` へ直接アクセス
- シークレットウィンドウで `/admin` へ直接アクセス
- `/signin-with-chatgpt` へ転送されないことを確認
- 認証CookieやChatGPTヘッダーなしでAPIが200を返すことを確認

### 11.4 レイアウトテスト

- 1920×1080
- 1366×768
- 1024×768
- 768×1024
- タッチ相当の狭い画面

重点確認:

- 待機カードの画像と能力バー
- DnD中の暗転範囲
- 4レーンのキャラクターと障害物
- ゴール付近の情報表示

---

## 12. リスクと対策

### JavaScript化による型安全性低下

- JSDocと実行時検証で重要データを保護する
- セッション、コース、API応答の自動テストを増やす

### DnD中の同期誤更新

- hover中は状態を書き換えない
- `onDragEnd` の成功時だけ `assignCharacter()` を呼ぶ
- ドラッグ開始時のセッション番号を保持し、途中でフェーズが変わった場合はキャンセルする

### タッチ操作とスクロールの競合

- 距離制約と長押し判定を使う
- クリック操作を必ず残す

### 背景演出による負荷

- DOM要素数を制限する
- 装飾は疑似要素とCSS背景を優先する
- React stateではなくCSSカスタムプロパティで視差を動かす
- 低スペック端末と `prefers-reduced-motion` 用の簡略表示を用意する

### 認証なし管理画面の公開

- v0.3ではユーザー要求に合わせて完全公開とする
- 共有状態が端末ローカルであることをREADMEへ明記する
- 将来は `/admin` の入口にだけ認証を追加できる構造を保つ

---

## 13. 最終受け入れ条件

### 言語・構成

- [ ] 製品コードと設定に `.ts` / `.tsx` / `.d.ts` が存在しない
- [ ] TypeScript関連依存が存在しない
- [ ] 全ルートがJS / JSX版でビルドされる

### 認証

- [ ] ChatGPT認証コードが存在しない
- [ ] 公開サイトが匿名アクセス可能
- [ ] `/game`、`/admin`、APIにログイン要求がない

### 管理画面

- [ ] キャラクターを左から右へドラッグできる
- [ ] ドラッグ中は右側ターゲットだけが明るい
- [ ] 空きレーン、置換、キャンセルが正しく動く
- [ ] クリック、タッチ、キーボードでも操作できる

### ゲーム画面

- [ ] 5区間の景観変化がある
- [ ] 3層以上の視差がある
- [ ] 障害物と当たり判定がデータで一元管理される
- [ ] キャラクターとゲーム情報が背景に埋もれない

### 参加待機画面

- [ ] カードが従来より縦長である
- [ ] イラストとSPD / ACC / JMP / STMが重ならない
- [ ] 全10体で名前と能力が読みやすい
- [ ] 主要5画面サイズでレイアウトが破綻しない

### 品質

- [ ] lint成功
- [ ] build成功
- [ ] 自動テスト成功
- [ ] 起動・操作テスト成功
- [ ] ブラウザコンソールエラー0件

---

## 14. v0.3で完成する体験

来場者画面は、手描き動物たちが学園祭の風景を駆け抜ける、区間変化と奥行きのあるレースになる。待機画面ではキャラクターと能力が重ならず、離れた場所からでも参加状況を確認できる。

スタッフ画面では、左のキャラクターを持ち上げて右のレーンへ直接運べる。ドラッグ中は配置先だけが明るくなるため、初見のスタッフでも操作を迷いにくい。

プロジェクト全体はJavaScript / JSXへ統一され、認証なしで起動・公開できる。将来認証を戻す場合も、ゲームや管理画面の操作ロジックを変更せず、入口のアクセス制御だけを追加できる状態とする。
